import PropTypes from "prop-types";

/**
 * Square component
 */
export const Square = (props) => {
  
  return (
    <div className="">
    </div>
  );
};

Square.propTypes = {
  type: PropTypes.string,
};

export default Square;